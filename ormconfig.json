{
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "username": "postgres",
  "password": "ca123",
  "database": "ts_ed_db",
  "synchronize": true,
  "logging": true,
  "entities": ["src/models/*.ts"],
  "migrations": ["src/migrations/*.ts"],
  "cli": {
    "migrationsDir": "src/migrations"
  }
}
